#include "Bank.h"
#include <iostream>
using namespace std;

int main() {
    Bank person;

    // map<int, vector<string>> s;
    // s[10] = {"11", "333", "33", "77"};
    // s[30] = {"1xx1", "3xxx", "x", "xxX"};
    // s[12] = {"jap", "sad", "sd", "sd"};
    // for (auto& d : s) {
    //     for (int i{}; i < d.second.size(); ++i) {
    //         if (i == d.second.size() - 1) {
    //             cout << d.second[i] << '\n';
    //         }
    //         else {
    //             cout << d.second[i] << ';';
    //         }
    //     }
    // }

    return 0;
}
